import { MerchantPosScreen } from '@/screens/MerchantPosScreen'

// Neutral "Tap to Pay" entry — anyone can accept a card tap into their wallet.
export default function TapRoute() {
  return <MerchantPosScreen />
}
