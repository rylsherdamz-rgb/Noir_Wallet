name: Contracts CI

on:
  push:
    branches: [main, develop, frontend]
    paths:
      - 'frontend/contracts/**'
      - 'frontend/contracts/*/Cargo.toml'
      - 'frontend/contracts/*/Cargo.lock'
  pull_request:
    branches: [main, frontend]
    paths:
      - 'frontend/contracts/**'
      - 'frontend/contracts/*/Cargo.toml'
      - 'frontend/contracts/*/Cargo.lock'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend/contracts/device_registry

    steps:
      - uses: actions/checkout@v4

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: wasm32-unknown-unknown
          components: clippy, rustfmt

      - uses: stellar-expert/soroban-cli-action@v1
        with:
          version: latest

      - run: cargo build --release --target wasm32-unknown-unknown
        name: Build Contracts

      - run: cargo test
        name: Run Tests

      - run: cargo clippy -- -D warnings
        name: Clippy

      - run: cargo fmt -- --check
        name: Format Check
